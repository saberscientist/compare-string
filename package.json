{
  "name": "compare-string",
  "version": "1.0.1",
  "description": "Pure JS package that compares similarity of strings using a combination of Jaccard's Index and the Sørensen–Dice Coefficient. Can generally return faster, more accurate results than edit-distance based algorithms, such as Levenshtein Distance.",
  "main": "jaccard.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["string similarity", "compare string", "fast string match", "levenshtein edit distance", "string difference"],
  "author": "saberscientist",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/saberscientist/compare-string.git"
  }
}